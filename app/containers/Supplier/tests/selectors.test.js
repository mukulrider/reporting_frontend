// import { fromJS } from 'immutable';
// import { makeSelectSupplierDomain } from '../selectors';

// const selector = makeSelectSupplierDomain();

describe('makeSelectSupplierDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
