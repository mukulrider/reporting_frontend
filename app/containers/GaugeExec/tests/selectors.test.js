// import { fromJS } from 'immutable';
// import { makeSelectGaugeExecDomain } from '../selectors';

// const selector = makeSelectGaugeExecDomain();

describe('makeSelectGaugeExecDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
