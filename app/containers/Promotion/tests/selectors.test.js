// import { fromJS } from 'immutable';
// import { makeSelectPromotionDomain } from '../selectors';

// const selector = makeSelectPromotionDomain();

describe('makeSelectPromotionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
