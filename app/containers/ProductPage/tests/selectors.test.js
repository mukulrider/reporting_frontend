// import { fromJS } from 'immutable';
// import { makeSelectProductPageDomain } from '../selectors';

// const selector = makeSelectProductPageDomain();

describe('makeSelectProductPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
