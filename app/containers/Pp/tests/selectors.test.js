// import { fromJS } from 'immutable';
// import { makeSelectPpDomain } from '../selectors';

// const selector = makeSelectPpDomain();

describe('makeSelectPpDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
