// import { fromJS } from 'immutable';
// import { makeSelectCompetitorDomain } from '../selectors';

// const selector = makeSelectCompetitorDomain();

describe('makeSelectCompetitorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
