// import { fromJS } from 'immutable';
// import { makeSelectExecutiveDomain } from '../selectors';

// const selector = makeSelectExecutiveDomain();

describe('makeSelectExecutiveDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
