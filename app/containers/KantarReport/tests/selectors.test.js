// import { fromJS } from 'immutable';
// import { makeSelectKantarReportDomain } from '../selectors';

// const selector = makeSelectKantarReportDomain();

describe('makeSelectKantarReportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
